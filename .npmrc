# Supply-chain hardening: do not run dependency lifecycle scripts on install.
# Own scripts (prepare = npm run build) are also skipped — CI and the release
# template run explicit build steps instead.
ignore-scripts=true
